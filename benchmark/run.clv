import std.io.*;
import std.sys.*;
import _cls.benchmark.*;

var env = Environment.new;

env.addLanguage('Python', 'python', 'py');
env.addLanguage('Lua', 'lua', 'lua');
println(env.langs);

var langs = env.langs;
langs.each(function(l) {
	println(l.name);
});
