import std.io as io;

Array< Array<Int> > mat;

Array<Int> row;
for (Int i = 0; i < 5; ++i) {
	row.push(i);
}

for (Int i = 0; i < 5; ++i) {
	mat.push(row);
}

for (Int i = 0; i < mat.size(); ++i) {
	for (Int j = 0; j < mat.at(i).size(); ++j) {
		io::print(mat.at(i).at(j));
	}
	
	io::print("\n");
}