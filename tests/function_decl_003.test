[FATAL] Testing a function which may not return a value #2
==CODE==
Int f(Int x) {
	if (x == 2) {
		return 3;
	}
	else if (x == 3) {
		return 5;
	}
}

==RESULT==
Compile error: Function `f' must return a value of type `Int', and it may not return on line \d+
