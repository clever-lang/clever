Testing Array<>::find()
==CODE==
import std.io;

Array<String> a;

a.push('Clever');
a.push('Programming');
a.push('Language');

println(a.find('Clever'));
println(a.find('Cleverasd'));
println(a.find('Language'));

Pair<Int, Bool> p1(1, true), p2(3, false), p3(-1, true), p4(-1, false);

Array<Pair<Int, Bool> > b;
b.push(p1);
b.push(p2);
b.push(p3);

println(b.find(p2));
println(b.find(p4));

==RESULT==
0
-1
2
1
-1
