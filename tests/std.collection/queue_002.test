Testing queue destruction
==CODE==
import std.io.*;
import std.collection.*;

var arr = [3, 2, 1];
var queue = Queue.new;

arr.each(function(n) { queue.push(n); });

println("Size: " + queue.size());
==RESULT==
Size: 3
