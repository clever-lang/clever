Testing stack destruction
==CODE==
import std.io.*;
import std.collection.*;

var arr = [3, 2, 1];
var stack = Stack.new;

arr.each(function(n) { stack.push(n); });

println("Size: " + stack.size());

==RESULT==
Size: 3
