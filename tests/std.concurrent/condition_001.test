Testing condition functionality
==CODE==
import std.io.*;
import std.concurrent.*;

var c = Condition.new();
var m = Mutex.new();

println(m.lock());

process thread {
	println(c.broadcast());
}

thread.run();
thread.wait();

println(c.wait(m));
==RESULT==
1
1
1
