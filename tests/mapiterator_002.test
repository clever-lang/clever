Testing Map.begin(), Map.end() and MapIterator class
==CODE==
import std.io.println;

Auto map = {'foo': 10, 'bar': -20, 'baz': 1010};
Auto e = map.end();

for (Auto b = map.begin(); b != e; ++b) {
	println(b.get());
}

==RESULT==
\(bar, -20\)
\(baz, 1010\)
\(foo, 10\)
