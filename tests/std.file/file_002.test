Testing Directories Manipulation
==CODE==
import std.io.*;
import std.file.*;

if (isdir('tests')) {
	println('Yep, its a dir!');
}

if (isdir('README.rst') == false) {
	println('Ops, its not a dir!');
}

println(isdir('dfasdfadsfadfadfadsfdasf'));

==RESULT==
Yep, its a dir!
Ops, its not a dir!
0
