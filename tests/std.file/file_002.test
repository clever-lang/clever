Testing Directories Manipulation
==CODE==
import std.io.*;
import std.file.*;

if (is_dir('tests')) {
	println('Yep, its a dir!');
}

if (is_dir('README.rst') == false) {
	println('Ops, its not a dir!');
}

is_dir('dfasdfadsfadfadfadsfdasf');

==RESULT==
Yep, its a dir!
Ops, its not a dir!
Fatal error: Unhandled exception!
Message: Directory does not exists.
