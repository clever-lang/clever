Testing ReflectionPackge->getModules()
==CODE==
import std.reflection as r;
import std.io;

use println as std.io::println;

r::ReflectionPackage package("std");

println(package.getModules().toString());
==RESULT==
\[[^\]]+\]
