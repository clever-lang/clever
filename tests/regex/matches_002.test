Testing Regex module assigning another pattern
==CODE==
import std.io.*;
import std.regex.* as re;

re::Regex a = /f+/;

println(a.matches("fffoo"));

a = /a+/;

println(a.matches("o"));
==RESULT==
true
false
